name: Deploy Netlify Production

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NETLIFY_AUTH_TOKEN_PRIMARY: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_AUTH_TOKEN_FALLBACK: ${{ secrets.NETLIFY_TOKEN }}
      NETLIFY_SITE_ID_PRIMARY: ${{ secrets.NETLIFY_SITE_ID }}
      NETLIFY_SITE_ID_FALLBACK: ${{ secrets.NETLIFY_SITE_ID_VILLALOSAPAMATES }}
    steps:
      - name: Checkout exact production commit
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies safely
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Run financial, payment, snapshot, close and security release gates
        run: npm test

      - name: Build the exact public artifact
        run: npm run build:public

      - name: Install Chromium for critical browser gates
        run: |
          npx playwright install --with-deps chromium

      - name: Run critical payment and admin browser gates
        run: node --test tests/owner-payment-report-browser.cjs tests/payment-duplicate-feedback-browser.cjs tests/admin-owner-access-browser.cjs tests/admin-premium-browser.cjs tests/admin-responsive-fouc-browser.cjs

      - name: Resolve Netlify credentials safely
        id: credentials
        shell: bash
        run: |
          AUTH_TOKEN="${NETLIFY_AUTH_TOKEN_PRIMARY:-${NETLIFY_AUTH_TOKEN_FALLBACK:-}}"
          SITE_ID="${NETLIFY_SITE_ID_PRIMARY:-${NETLIFY_SITE_ID_FALLBACK:-}}"
          if [ -n "$AUTH_TOKEN" ]; then echo "::add-mask::$AUTH_TOKEN"; fi
          if [ -n "$SITE_ID" ]; then echo "::add-mask::$SITE_ID"; fi
          if [ -z "$AUTH_TOKEN" ] || [ -z "$SITE_ID" ]; then
            echo "ready=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "ready=true" >> "$GITHUB_OUTPUT"
          echo "NETLIFY_AUTH_TOKEN=$AUTH_TOKEN" >> "$GITHUB_ENV"
          echo "NETLIFY_SITE_ID=$SITE_ID" >> "$GITHUB_ENV"

      - name: Inspect Netlify production configuration
        id: site
        if: steps.credentials.outputs.ready == 'true'
        shell: bash
        run: |
          site_json="$(curl --fail-with-body --silent --show-error \
            -H "Authorization: Bearer $NETLIFY_AUTH_TOKEN" \
            -H "User-Agent: VLA-GitHub-Actions" \
            "https://api.netlify.com/api/v1/sites/$NETLIFY_SITE_ID")"
          production_branch="$(printf '%s' "$site_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);process.stdout.write(String(j.build_settings?.repo_branch||j.repo?.branch||'unknown'))})")"
          repo_url="$(printf '%s' "$site_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);process.stdout.write(String(j.build_settings?.repo_url||j.repo?.repo_path||'connected'))})")"
          echo "production_branch=$production_branch" >> "$GITHUB_OUTPUT"
          echo "Production branch: $production_branch"
          echo "Repository connection: $repo_url"

      - name: Trigger server-side production build
        id: trigger
        if: steps.site.outcome == 'success'
        shell: bash
        run: |
          build_json="$(curl --fail-with-body --silent --show-error --request POST \
            -H "Authorization: Bearer $NETLIFY_AUTH_TOKEN" \
            -H "User-Agent: VLA-GitHub-Actions" \
            "https://api.netlify.com/api/v1/sites/$NETLIFY_SITE_ID/builds?clear_cache=true")"
          build_id="$(printf '%s' "$build_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);process.stdout.write(String(j.id||j.build_id||''))})")"
          if [ -z "$build_id" ]; then
            echo "Netlify did not return a build ID."
            exit 1
          fi
          echo "build_id=$build_id" >> "$GITHUB_OUTPUT"
          echo "Server-side production build triggered: $build_id"

      - name: Verify exact production deploy and canonical release contract
        id: verify
        if: steps.trigger.outcome == 'success'
        env:
          BUILD_ID: ${{ steps.trigger.outputs.build_id }}
        shell: bash
        run: |
          production_release="${RUNNER_TEMP}/vla-production-release.json"
          for attempt in $(seq 1 90); do
            build_json="$(curl --fail --silent --show-error \
              -H "Authorization: Bearer $NETLIFY_AUTH_TOKEN" \
              -H "User-Agent: VLA-GitHub-Actions" \
              "https://api.netlify.com/api/v1/builds/$BUILD_ID" 2>/dev/null || true)"
            if [ -n "$build_json" ]; then
              build_state="$(printf '%s' "$build_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);process.stdout.write(String(j.state||(j.done?'done':'pending')))})")"
              build_error="$(printf '%s' "$build_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const e=j.error;process.stdout.write(e?String(e):'')})")"
              echo "Attempt $attempt: Netlify build state $build_state"
              if [ -n "$build_error" ]; then
                echo "Netlify build error: $build_error"
                exit 1
              fi
            fi

            deploy_json="$(curl --fail --silent --show-error \
              -H "Authorization: Bearer $NETLIFY_AUTH_TOKEN" \
              -H "User-Agent: VLA-GitHub-Actions" \
              "https://api.netlify.com/api/v1/sites/$NETLIFY_SITE_ID/deploys?per_page=20" 2>/dev/null || true)"
            deploy_id="$(printf '%s' "$deploy_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s||'[]');const d=a.find(x=>x.context==='production'&&x.commit_ref===process.env.GITHUB_SHA);process.stdout.write(String(d?.id||''))})" 2>/dev/null || true)"
            deploy_state="$(printf '%s' "$deploy_json" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s||'[]');const d=a.find(x=>x.context==='production'&&x.commit_ref===process.env.GITHUB_SHA);process.stdout.write(String(d?.state||''))})" 2>/dev/null || true)"
            if [ -n "$deploy_id" ]; then
              echo "Attempt $attempt: exact commit deploy $deploy_id is $deploy_state"
            fi
            if [ "$deploy_state" = "ready" ] && curl --fail --silent --show-error --location --max-time 30 \
              "https://villalosapamates.netlify.app/release.json?commit=$GITHUB_SHA&attempt=$attempt" \
              --output "$production_release"; then
              if node scripts/verify-release-contract.js release.json "$production_release"; then
                echo "deploy_id=$deploy_id" >> "$GITHUB_OUTPUT"
                echo "Production verified for exact commit $GITHUB_SHA with canonical release.json."
                exit 0
              fi
            fi
            sleep 10
          done
          echo "Production did not expose the exact commit and canonical release contract before timeout."
          exit 1

      - name: Report deployment result
        if: always()
        shell: bash
        env:
          CREDENTIALS_READY: ${{ steps.credentials.outputs.ready }}
          SITE_OUTCOME: ${{ steps.site.outcome }}
          TRIGGER_OUTCOME: ${{ steps.trigger.outcome }}
          VERIFY_OUTCOME: ${{ steps.verify.outcome }}
          PRODUCTION_BRANCH: ${{ steps.site.outputs.production_branch }}
          BUILD_ID: ${{ steps.trigger.outputs.build_id }}
          DEPLOY_ID: ${{ steps.verify.outputs.deploy_id }}
        run: |
          if [ "$CREDENTIALS_READY" != "true" ]; then
            title="Producción Netlify bloqueada"
            detail="Los secretos de Netlify no estuvieron disponibles. No se modificó producción."
          elif [ "$TRIGGER_OUTCOME" = "success" ] && [ "$VERIFY_OUTCOME" = "success" ]; then
            title="Contrato canónico publicado y verificado ✅"
            detail="Netlify confirmó el commit exacto y release.json coincide clave por clave con el artefacto servido."
          else
            title="Producción no verificada ⚠️"
            detail="El despliegue o la verificación canónica no concluyeron correctamente."
          fi
          {
            echo "### $title"
            echo
            echo "$detail"
            echo
            echo "Rama: \`${PRODUCTION_BRANCH:-unknown}\` · Build: \`${BUILD_ID:-not-returned}\` · Deploy: \`${DEPLOY_ID:-not-returned}\` · Commit: \`$GITHUB_SHA\`"
          } >> "$GITHUB_STEP_SUMMARY"

'use strict';
const test=require('node:test');const assert=require('node:assert/strict');
const discovery=require('../netlify/functions/_shared/_payment_ai_model_discovery');
test('elige Flash estable completo y excluye preview para fallback financiero',()=>{const model=discovery.chooseCompatibleModel([{name:'models/text-embedding',supportedGenerationMethods:['embedContent']},{name:'models/gemini-9.9-flash-preview',supportedGenerationMethods:['generateContent']},{name:'models/gemini-2.5-flash',baseModelId:'gemini-2.5-flash',supportedGenerationMethods:['generateContent']},{name:'models/gemini-3.5-flash-lite',baseModelId:'gemini-3.5-flash-lite',supportedGenerationMethods:['generateContent']}]);assert.equal(model,'gemini-2.5-flash');assert.equal(discovery.score({baseModelId:'gemini-9.9-flash-preview',supportedGenerationMethods:['generateContent']}),-1)});
test('consulta y guarda el catálogo una sola vez',async()=>{discovery.clearMemoryCache();let fetches=0,stored=null;const store={get:async()=>stored,setJSON:async(_key,value)=>{stored=value}};const options={apiKey:'clave-de-prueba',fetchFn:async()=>{fetches++;return{ok:true,json:async()=>({models:[{baseModelId:'gemini-3.5-flash',supportedGenerationMethods:['generateContent']} ]})}},storeFactory:async()=>store,now:()=>1000};const first=await discovery.discoverCompatibleModel(options),second=await discovery.discoverCompatibleModel(options);assert.equal(first.model,'gemini-3.5-flash');assert.equal(second.model,'gemini-3.5-flash');assert.equal(fetches,1)});

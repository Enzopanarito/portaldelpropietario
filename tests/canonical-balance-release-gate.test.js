'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');
const {compactOwner}=require('../netlify/functions/public-data-v2');
test('el DTO público expone un único contrato financiero sin alterar campos históricos',()=>{const record={id:'recOwner00000001',fields:{Casa:8,Propietario:'Casa 8',Alicuota:.1,'Estado Acceso Portón':'Habilitado'}},balance={usd:70,bsRef:-.03,totalRef:69.97,expiredUsd:20,expiredBsRef:0,currentUsd:50,currentBsRef:-.03};const dto=compactOwner(record,balance);assert.equal(dto.saldoUsd,70);assert.equal(dto.saldoBsRef,-.03);assert.equal(dto.totalPagadero,70);assert.equal(dto.saldoNetoReferencial,69.97);assert.equal(dto.saldoFavorBs,.03);assert.equal(dto['Saldo Total Actual'],69.97);assert.equal(dto.balanceEngineVersion,'vla-balance-contract-v7')});
test('el release no activa parches históricos sobre saldos, Health o aprobación de pagos',()=>{const config=fs.readFileSync('netlify.toml','utf8'),pwa=fs.readFileSync('netlify/edge-functions/pwa-head.js','utf8');assert(!/function = "(?:balance-fix|currency-balance-fix|accounting-health-fix|admin-payment-flow)"/.test(config));assert(!pwa.includes("Deuda Restante")&&!pwa.includes('function calc('));assert(fs.readFileSync('index.html','utf8').includes('/vla-finance-v7.js'));assert(fs.readFileSync('admin.html','utf8').includes('/vla-finance-v7.js'))});

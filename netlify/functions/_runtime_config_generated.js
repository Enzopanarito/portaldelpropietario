'use strict';

// Este archivo se regenera durante cada build de Netlify.
// El valor versionado es deliberadamente seguro para desarrollo y CI.
module.exports=Object.freeze({
 schemaVersion:1,
 generated:false,
 deployContext:'local',
 publicBlobCacheEnabled:false,
 publicBlobCacheMaxAgeMs:120000,
 dataEnvironment:'legacy'
});

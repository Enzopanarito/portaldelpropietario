diff --git a/admin.html b/admin.html
index a72e456..d56177f 100644
--- a/admin.html
+++ b/admin.html
@@ -47,15 +47,15 @@ function renderOwners(list){document.getElementById('owners-body').innerHTML=lis
 function renderExpenses(){document.getElementById('expenses-body').innerHTML=gastos.map(g=>{const f=g.fields||{},n=(f.Propietarios||[]).length||1,part=f['Tipo de Gasto']==='Gasto Especial'?Number(f.Monto||0)/n:Number(f.Monto||0);return`<tr class='border-b'><td class='p-3'><input type='checkbox' class='expense-cb' value='${g.id}'></td><td class='p-3'>${f.Concepto||''}<br><span class='text-xs text-slate-500'>${f['Tipo de Gasto']||''} · ${n} propietario(s)</span></td><td class='p-3 text-right'>${usd(f.Monto||0)}</td><td class='p-3 text-right'>${usd(part)}</td><td class='p-3'>${f['Forma de Pago']||'Bs BCV'}</td></tr>`}).join('')||'<tr><td colspan="5" class="p-4 text-center">Sin gastos</td></tr>';updateDeleteBtn()}
 function renderChecks(){document.getElementById('owners-list').innerHTML=owners.map(o=>`<label class='block'><input type='checkbox' value='${o.id}' checked> Casa ${o.Casa} - ${o.Propietario||''}</label>`).join('')}
 function renderReports(){const pending=reportes.filter(r=>(r.fields||{}).Estado==='Pendiente');const badge=document.getElementById('badge');badge.textContent=pending.length;badge.classList.toggle('hidden',!pending.length);badge.classList.toggle('flex',!!pending.length);document.getElementById('reports-body').innerHTML=pending.map(r=>{const f=r.fields||{},oid=(f['Propietario que Reporta']||[])[0],o=owners.find(x=>x.id===oid),mode=f['Forma de Pago Reportada']||'Bs BCV';const amt=mode==='USD'?usd(f['Monto Reportado']):`${bs(f['Monto Reportado Bs']||0)} / ${usd(f['Equivalente USD Reportado']||f['Monto Reportado']||0)} ref.`;return`<tr class='border-b'><td class='p-3'>${o?`Casa ${o.Casa} - ${o.Propietario}`:'N/D'}</td><td class='p-3'>${mode}</td><td class='p-3 text-right font-semibold'>${amt}</td><td class='p-3'>${f.Referencia||''}</td><td class='p-3'><button class='confirm-report bg-green-100 text-green-800 px-3 py-1 rounded-full text-xs' data-id='${r.id}'>Confirmar</button> <button class='reject-report bg-red-100 text-red-800 px-3 py-1 rounded-full text-xs' data-id='${r.id}'>Rechazar</button></td></tr>`}).join('')||'<tr><td colspan="5" class="p-4 text-center">No hay pagos pendientes</td></tr>'}
-function setupEvents(){if(window.ready)return;window.ready=true;document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>{document.querySelectorAll('.nav,.section').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById(b.dataset.target).classList.add('active');if(b.dataset.target==='health')loadHealth()});document.getElementById('refresh-btn').onclick=()=>loadAll(true);document.getElementById('usage-btn').onclick=loadUsage;document.getElementById('health-refresh').onclick=loadHealth;document.getElementById('backup-btn').onclick=downloadBackup;document.getElementById('close-btn').onclick=runClose;document.getElementById('search').oninput=e=>{const q=e.target.value.toLowerCase();renderOwners(owners.filter(o=>String(o.Casa||'').includes(q)||(o.Propietario||'').toLowerCase().includes(q)))};document.getElementById('owners-body').onclick=e=>{const b=e.target.closest('.pay-btn');if(b)showPay(b.dataset.id)};document.getElementById('pay-close').onclick=hidePay;document.getElementById('pay-cancel').onclick=hidePay;document.getElementById('pay-mode').onchange=payNote;document.getElementById('pay-confirm').onclick=manualPay;document.getElementById('expense-form').onsubmit=addExpense;document.getElementById('expense-type').onchange=e=>{document.getElementById('owners-checks').style.display=e.target.value==='Gasto Común'?'none':'block'};document.getElementById('toggle-owners').onclick=e=>{const boxes=[...document.querySelectorAll('#owners-list input')],all=boxes.every(x=>x.checked);boxes.forEach(x=>x.checked=!all);e.target.textContent=all?'Seleccionar todos':'Deseleccionar todos'};document.getElementById('expenses-body').onchange=e=>{if(e.target.classList.contains('expense-cb'))updateDeleteBtn()};document.getElementById('select-all-expenses').onchange=e=>{document.querySelectorAll('.expense-cb').forEach(x=>x.checked=e.target.checked);updateDeleteBtn()};document.getElementById('delete-expenses').onclick=deleteExpenses;document.getElementById('reports-body').onclick=handleReport}
+function setupEvents(){if(window.ready)return;window.ready=true;document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>{document.querySelectorAll('.nav,.section').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById(b.dataset.target).classList.add('active');if(b.dataset.target==='health')loadHealth()});document.getElementById('refresh-btn').onclick=()=>loadAll(true);document.getElementById('usage-btn').onclick=loadUsage;document.getElementById('health-refresh').onclick=loadHealth;document.getElementById('backup-btn').onclick=downloadBackup;document.getElementById('close-btn').onclick=runClose;document.getElementById('search').oninput=e=>{const q=e.target.value.toLowerCase();renderOwners(owners.filter(o=>String(o.Casa||'').includes(q)||(o.Propietario||'').toLowerCase().includes(q)))};document.getElementById('owners-body').onclick=e=>{const b=e.target.closest('.pay-btn');if(b)showPay(b.dataset.id)};document.getElementById('pay-close').onclick=hidePay;document.getElementById('pay-cancel').onclick=hidePay;document.getElementById('pay-mode').onchange=payNote;document.getElementById('pay-amount').oninput=payNote;document.getElementById('pay-confirm').onclick=manualPay;document.getElementById('expense-form').onsubmit=addExpense;document.getElementById('expense-type').onchange=e=>{document.getElementById('owners-checks').style.display=e.target.value==='Gasto Común'?'none':'block'};document.getElementById('toggle-owners').onclick=e=>{const boxes=[...document.querySelectorAll('#owners-list input')],all=boxes.every(x=>x.checked);boxes.forEach(x=>x.checked=!all);e.target.textContent=all?'Seleccionar todos':'Deseleccionar todos'};document.getElementById('expenses-body').onchange=e=>{if(e.target.classList.contains('expense-cb'))updateDeleteBtn()};document.getElementById('select-all-expenses').onchange=e=>{document.querySelectorAll('.expense-cb').forEach(x=>x.checked=e.target.checked);updateDeleteBtn()};document.getElementById('delete-expenses').onclick=deleteExpenses;document.getElementById('reports-body').onclick=handleReport}
 function updateDeleteBtn(){const c=document.querySelectorAll('.expense-cb:checked').length,b=document.getElementById('delete-expenses');b.disabled=!c;b.textContent=c?`Eliminar (${c})`:'Eliminar'}
 function showPay(id){currentOwnerId=id;const o=owners.find(x=>x.id===id),c=calc(o);document.getElementById('pay-info').innerHTML=`Casa ${o.Casa} - <b>${o.Propietario}</b><br>USD: <b>${usd(Math.max(0,c.debtUsd))}</b> · Bs: <b>${usd(Math.max(0,c.debtBs))}</b> / <b>${bs(c.bsDue)}</b>${c.total<0?`<br><span class='text-green-600 font-bold'>${saldoFavorText(c)}</span>`:''}`;document.getElementById('pay-amount').value='';payNote();document.getElementById('pay-modal').classList.remove('hidden');document.getElementById('pay-modal').classList.add('flex')}
 function hidePay(){document.getElementById('pay-modal').classList.add('hidden');document.getElementById('pay-modal').classList.remove('flex')}
-function payNote(){document.getElementById('pay-note').textContent=document.getElementById('pay-mode').value==='USD'?'Se aplicará como pago exclusivamente en dólares.':`Se registrará en Bs y equivalente USD a tasa ${bcv&&bcv.rateFormatted?bcv.rateFormatted:bs(rate())}.`}
-async function manualPay(){try{const mode=document.getElementById('pay-mode').value,amount=Number(document.getElementById('pay-amount').value);if(!currentOwnerId||!(amount>0))throw new Error('Ingrese un monto válido.');const usdEq=mode==='Bs BCV'?(rate()?money(amount/rate()):0):amount;const fields={'Propietario que Paga':[currentOwnerId],'Fecha de Pago':caracasDate(),'Forma de Pago':mode,'Monto Pagado':usdEq,'Equivalente USD Aplicado':usdEq};if(mode==='Bs BCV'){fields['Monto Pagado Bs']=amount;fields['Tasa BCV Aplicada']=rate()}await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_PAGOS),{method:'POST',body:JSON.stringify({records:[{fields}],typecast:true})});hidePay();toast('Pago registrado.');loadAll(true)}catch(e){toast(e.message,true)}}
-async function addExpense(e){e.preventDefault();try{const concept=document.getElementById('expense-concept').value.trim(),amount=Number(document.getElementById('expense-amount').value),type=document.getElementById('expense-type').value,mode=document.getElementById('expense-mode').value,freq=document.getElementById('expense-frequency').value;if(!concept||!(amount>0))throw new Error('Complete concepto y monto.');const selected=type==='Gasto Común'?owners.map(o=>o.id):[...document.querySelectorAll('#owners-list input:checked')].map(x=>x.value);if(!selected.length)throw new Error('Seleccione propietarios.');await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_GASTOS),{method:'POST',body:JSON.stringify({records:[{fields:{Concepto:concept,Monto:amount,'Tipo de Gasto':type,Frecuencia:freq,Propietarios:selected,'Forma de Pago':mode}}],typecast:true})});toast(type==='Gasto Especial'?`Gasto especial creado: ${usd(amount)} entre ${selected.length} propietario(s).`:'Gasto común creado.');e.target.reset();document.querySelectorAll('#owners-list input').forEach(x=>x.checked=true);loadAll(true)}catch(err){toast(err.message,true)}}
+function payNote(){const mode=document.getElementById('pay-mode').value,amount=Number(document.getElementById('pay-amount').value||0),r=rate(),note=document.getElementById('pay-note');if(mode==='USD'){note.textContent='Ingrese el monto en USD referencial. Se aplicará como pago en dólares.';return}const bsEq=(amount>0&&r>0)?money(amount*r):0;note.textContent=`Ingrese el monto en USD referencial. Se registrará como pago en bolívares a tasa ${bcv&&bcv.rateFormatted?bcv.rateFormatted:bs(r)}${amount>0?` · Equivalente: ${bs(bsEq)}`:''}`;}
+async function manualPay(){if(window.vlaPayBusy)return;const btn=document.getElementById('pay-confirm');try{const mode=document.getElementById('pay-mode').value,amount=Number(document.getElementById('pay-amount').value),owner=owners.find(x=>x.id===currentOwnerId),r=rate();if(!currentOwnerId||!owner)throw new Error('Seleccione un propietario.');if(!(amount>0))throw new Error('Ingrese un monto válido en USD referencial.');if(mode==='Bs BCV'&&!(r>0))throw new Error('No hay tasa BCV disponible. Presione Actualizar e intente de nuevo.');window.vlaPayBusy=true;btn.disabled=true;btn.textContent='Registrando...';const processed=await adminFetch('/.netlify/functions/admin-manual-payment',{method:'POST',body:JSON.stringify({ownerId:currentOwnerId,mode,amount,rate:r,reference:'Pago manual admin'})});hidePay();toast(processed.message||'Pago registrado.');await loadAll(true)}catch(e){toast(e.message,true)}finally{window.vlaPayBusy=false;btn.disabled=false;btn.textContent='Registrar'}}
+async function addExpense(e){e.preventDefault();try{const concept=document.getElementById('expense-concept').value.trim(),amount=Number(document.getElementById('expense-amount').value),type=document.getElementById('expense-type').value,mode=document.getElementById('expense-mode').value,freq=document.getElementById('expense-frequency').value;if(!concept||!(amount>0))throw new Error('Complete concepto y monto.');const selected=type==='Gasto Común'?owners.map(o=>o.id):[...document.querySelectorAll('#owners-list input:checked')].map(x=>x.value);if(!selected.length)throw new Error('Seleccione propietarios.');const result=await adminFetch('/.netlify/functions/admin-expense',{method:'POST',body:JSON.stringify({action:'create',concept,amount,type,mode,frequency:freq,ownerIds:selected})});toast(result.message||(type==='Gasto Especial'?`Gasto especial creado: ${usd(amount)} entre ${selected.length} propietario(s).`:'Gasto común creado.'));e.target.reset();document.querySelectorAll('#owners-list input').forEach(x=>x.checked=true);await loadAll(true)}catch(err){toast(err.message,true)}}
 async function deleteExpenses(){try{const ids=[...document.querySelectorAll('.expense-cb:checked')].map(x=>x.value);if(!ids.length)return;if(!confirm(`¿Eliminar ${ids.length} gasto(s)?`))return;await adminFetch('/.netlify/functions/batch-delete-records',{method:'POST',body:JSON.stringify({tableName:TABLE_GASTOS,recordIds:ids})});toast('Gastos eliminados.');loadAll(true)}catch(e){toast(e.message,true)}}
-async function handleReport(e){const btn=e.target.closest('button');if(!btn)return;const id=btn.dataset.id,r=reportes.find(x=>x.id===id);if(!r)return;try{if(btn.classList.contains('confirm-report')){const f=r.fields||{},mode=f['Forma de Pago Reportada']||'Bs BCV',ownerId=(f['Propietario que Reporta']||[])[0],usdEq=Number(f['Equivalente USD Reportado']||f['Monto Reportado']||0);const fields={'Propietario que Paga':[ownerId],'Fecha de Pago':caracasDate(),'Forma de Pago':mode,'Monto Pagado':usdEq,'Equivalente USD Aplicado':usdEq};if(mode==='Bs BCV'){fields['Monto Pagado Bs']=Number(f['Monto Reportado Bs']||0);fields['Tasa BCV Aplicada']=Number(f['Tasa BCV Reporte']||0)}await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_PAGOS),{method:'POST',body:JSON.stringify({records:[{fields}],typecast:true})});await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_REPORTES)+'/'+id,{method:'PATCH',body:JSON.stringify({fields:{Estado:'Confirmado'}})});toast('Pago confirmado.')}else{if(!confirm('¿Rechazar este pago?'))return;await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_REPORTES)+'/'+id,{method:'PATCH',body:JSON.stringify({fields:{Estado:'Rechazado'}})});toast('Pago rechazado.')}loadAll(true)}catch(err){toast(err.message,true)}}
+async function handleReport(e){const btn=e.target.closest('button');if(!btn||window.vlaReportBusy)return;const id=btn.dataset.id,r=reportes.find(x=>x.id===id);if(!r)return;const approve=btn.classList.contains('confirm-report'),reject=btn.classList.contains('reject-report');if(!approve&&!reject)return;if(reject&&!confirm('¿Rechazar este pago?'))return;const original=btn.textContent;try{window.vlaReportBusy=true;btn.disabled=true;btn.textContent=approve?'Confirmando...':'Rechazando...';const processed=await adminFetch('/.netlify/functions/process-payment-report',{method:'POST',body:JSON.stringify({reportId:id,decision:approve?'approve':'reject'})});toast(processed.message||(approve?'Pago confirmado, recibo procesado y acceso sincronizado.':'Pago rechazado y acceso sincronizado.'));await loadAll(true)}catch(err){toast(err.message,true)}finally{window.vlaReportBusy=false;btn.disabled=false;btn.textContent=original}}
 function closeRows(){return owners.map(o=>{const c=calc(o);return`<tr class='border-b'><td class='p-2 font-bold'>${o.Casa||''}</td><td class='p-2'>${o.Propietario||''}</td><td class='p-2 text-right'>${c.debtUsd<-.01?'A favor '+usd(Math.abs(c.debtUsd)):usd(Math.max(0,c.debtUsd))}</td><td class='p-2 text-right'>${c.debtBs<-.01?'A favor Bs Ref. '+usd(Math.abs(c.debtBs)):usd(Math.max(0,c.debtBs))}</td><td class='p-2 text-right font-bold ${c.total>0?'text-red-600':'text-green-600'}'>${saldoFavorText(c)}</td></tr>`}).join('')}
 function updateCloseConfirm(){const ok=[...document.querySelectorAll('.close-check')].every(x=>x.checked);document.getElementById('close-review-confirm').disabled=!ok}
 function showCloseReview(v){return new Promise(resolve=>{document.querySelectorAll('.close-check').forEach(x=>{x.checked=false;x.onchange=updateCloseConfirm});updateCloseConfirm();document.getElementById('close-summary').innerHTML=`<div class='grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3 text-center'><div class='bg-slate-100 p-3 rounded-xl'><p class='text-xs text-slate-500'>Mes</p><b>${v.month||''}</b></div><div class='bg-slate-100 p-3 rounded-xl'><p class='text-xs text-slate-500'>USD</p><b>${usd(v.totalUsd)}</b></div><div class='bg-slate-100 p-3 rounded-xl'><p class='text-xs text-slate-500'>Bs Ref.</p><b>${usd(v.totalBsRef)}</b></div><div class='bg-slate-100 p-3 rounded-xl'><p class='text-xs text-slate-500'>Total Ref.</p><b>${usd(v.totalRef)}</b></div></div><div class='mt-3 bg-${v.transitionMode?'amber':'green'}-50 border-l-4 border-${v.transitionMode?'amber':'green'}-500 p-3 rounded text-sm'><b>${v.transitionMode?'Modo transición activo':'Modo doble moneda limpio'}.</b> Pagos a cerrar: ${v.pendingPaymentsCount||0}. Diferencias cálculo/legacy: ${v.differenceCount||0}. Con saldo a favor: ${v.conSaldoFavor||0}.</div>`;document.getElementById('close-owner-table').innerHTML=`<h4 class='font-bold mb-2'>Detalle por propietario</h4><div class='overflow-x-auto border rounded-lg max-h-80'><table class='min-w-full text-sm'><thead class='bg-slate-100 sticky top-0'><tr><th class='p-2 text-left'>Casa</th><th class='p-2 text-left'>Propietario</th><th class='p-2 text-right'>USD</th><th class='p-2 text-right'>Bs Ref.</th><th class='p-2 text-right'>Resultado cierre</th></tr></thead><tbody>${closeRows()}</tbody></table></div>`;const modal=document.getElementById('close-modal');modal.classList.remove('hidden');modal.classList.add('flex');const done=ok=>{modal.classList.add('hidden');modal.classList.remove('flex');resolve(ok)};document.getElementById('close-review-x').onclick=()=>done(false);document.getElementById('close-review-cancel').onclick=()=>done(false);document.getElementById('close-review-confirm').onclick=()=>done(true)})}

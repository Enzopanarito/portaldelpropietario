diff --git a/mkj-access.html b/mkj-access.html
index f9e1c54..860fbc6 100644
--- a/mkj-access.html
+++ b/mkj-access.html
@@ -54,7 +54,7 @@ async function loadMode(){try{const d=await adminFetch('/.netlify/functions/acce
 async function saveMode(){try{const mode=document.getElementById('access-mode').value;if(mode==='Manual'&&!confirm('¿Confirmas poner el portón en modo Manual? No habrá limitaciones ni habilitaciones automáticas.'))return;const d=await adminFetch('/.netlify/functions/access-mode',{method:'POST',body:JSON.stringify({mode})});accessMode=d.mode;renderMode();toast(d.message||'Modo actualizado.')}catch(e){toast(e.message,true)}}
 async function loadOwners(){try{const d=await adminFetch('/.netlify/functions/admin-data?force=1');owners=(d.propietarios||[]).sort((a,b)=>(a.Casa||0)-(b.Casa||0));document.getElementById('owner').innerHTML='<option value="">Seleccione...</option>'+owners.map(o=>`<option value="${o.id}">Casa ${o.Casa} - ${o.Propietario} - ${o['Estado Acceso Portón']||'Sin configurar'}</option>`).join('');toast('Propietarios cargados.')}catch(e){toast(e.message,true)}}
 function selectOwner(){const id=document.getElementById('owner').value;current=owners.find(o=>o.id===id);document.getElementById('owner-card').classList.toggle('hidden',!current);if(!current)return;document.getElementById('owner-title').textContent=`Casa ${current.Casa} - ${current.Propietario}`;document.getElementById('owner-debt').textContent=`Deuda total actual: ${money(current['Deuda Restante'])} · Deuda vencida USD: ${money(current['Deuda Anterior USD']||0)} · Deuda vencida Bs Ref.: ${money(current['Deuda Anterior Bs Ref']||current['Deuda Anterior']||0)}`;document.getElementById('mkj-id').value=current['MKJ User ID']||'';document.getElementById('mkj-email').value=current['MKJ Email']||current.Email||'';document.getElementById('owner-status').innerHTML=`Estado actual: <b>${current['Estado Acceso Portón']||'Sin configurar'}</b> · Última sync: ${current['Última Sync MKJ']||'—'}`}
-async function saveIds(){if(!current)return;const fields={'MKJ User ID':document.getElementById('mkj-id').value.trim(),'MKJ Email':document.getElementById('mkj-email').value.trim()};try{await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent('Propietarios')+'/'+current.id,{method:'PATCH',body:JSON.stringify({fields,typecast:true})});toast('IDs MKJ guardados.');await loadOwners();document.getElementById('owner').value=current.id;selectOwner()}catch(e){toast(e.message,true)}}
+async function saveIds(){if(!current)return;const mkjUserId=document.getElementById('mkj-id').value.trim(),mkjEmail=document.getElementById('mkj-email').value.trim();try{const d=await adminFetch('/.netlify/functions/mkj-access',{method:'POST',body:JSON.stringify({action:'save-identifiers',ownerId:current.id,mkjUserId,mkjEmail})});toast(d.message||'IDs MKJ guardados.');await loadOwners();document.getElementById('owner').value=current.id;selectOwner()}catch(e){toast(e.message,true)}}
 async function doAction(action){if(!current)return;const mkjUserId=document.getElementById('mkj-id').value.trim();if(!mkjUserId)return toast('Falta MKJ User ID.',true);const label=action==='enable'?'habilitar':'limitar';if(!confirm(`¿Confirmas ${label} el acceso de Casa ${current.Casa}?`))return;try{const d=await adminFetch('/.netlify/functions/mkj-access',{method:'POST',body:JSON.stringify({action,ownerId:current.id,mkjUserId,reason:document.getElementById('reason').value.trim()})});document.getElementById('result').classList.remove('hidden');document.getElementById('result-text').textContent=JSON.stringify(d,null,2);toast(d.message||'Sincronizado.');await loadOwners();document.getElementById('owner').value=current.id;selectOwner()}catch(e){toast(e.message,true)}}
 async function testLogin(){try{const d=await adminFetch('/.netlify/functions/mkj-access',{method:'POST',body:JSON.stringify({action:'test-login'})});document.getElementById('result').classList.remove('hidden');document.getElementById('result-text').textContent=JSON.stringify(d,null,2);toast('Login MKJ correcto.')}catch(e){toast(e.message,true)}}
 async function autoSync(){if(accessMode==='Manual')return toast('El sistema está en modo Manual. Cambie a Automático para ejecutar Auto Sync.',true);if(!confirm('¿Ejecutar sincronización automática para todos los propietarios según deuda vencida anterior?'))return;try{const d=await adminFetch('/.netlify/functions/access-auto-sync',{method:'POST',body:JSON.stringify({sendEmail:true})});document.getElementById('result').classList.remove('hidden');document.getElementById('result-text').textContent=JSON.stringify(d,null,2);toast(`Auto Sync listo. Limitados: ${d.limited||0}, habilitados: ${d.enabled||0}, errores: ${d.errors||0}`);await loadOwners()}catch(e){toast(e.message,true)}}
diff --git a/netlify.toml b/netlify.toml
index 03187f2..140d2dc 100644
--- a/netlify.toml
+++ b/netlify.toml
@@ -67,3 +67,6 @@ for = "/service-worker.js"
 for = "/release.json"
   [headers.values]
   Cache-Control = "no-store, no-cache, must-revalidate"
+
+[functions."whatsapp-scheduler"]
+  schedule = "*/5 * * * *"
diff --git a/netlify/edge-functions/admin-links.js b/netlify/edge-functions/admin-links.js
index 1a1244a..f4bfbac 100644
--- a/netlify/edge-functions/admin-links.js
+++ b/netlify/edge-functions/admin-links.js
@@ -100,14 +100,7 @@ export default async (request, context) => {
     "async function manualPay(){if(window.vlaPayBusy)return;const btn=document.getElementById('pay-confirm');try{const mode=document.getElementById('pay-mode').value,amount=Number(document.getElementById('pay-amount').value),owner=owners.find(x=>x.id===currentOwnerId),r=rate();if(!currentOwnerId||!owner)throw new Error('Seleccione un propietario.');if(!(amount>0))throw new Error('Ingrese un monto válido en USD referencial.');if(mode==='Bs BCV'&&!(r>0))throw new Error('No hay tasa BCV disponible. Presione Actualizar e intente de nuevo.');window.vlaPayBusy=true;btn.disabled=true;btn.textContent='Registrando...';const processed=await adminFetch('/.netlify/functions/admin-manual-payment',{method:'POST',body:JSON.stringify({ownerId:currentOwnerId,mode,amount,rate:r,reference:'Pago manual admin'})});hidePay();toast(processed.message||'Pago registrado.');await loadAll(true)}catch(e){toast(e.message,true)}finally{window.vlaPayBusy=false;btn.disabled=false;btn.textContent='Registrar'}}"
   );
 
-  // Confirmar/rechazar reportes siempre debe pasar por backend para crear pago, recibo PDF/correo y sincronizar portón.
-  const reportOld = "await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_PAGOS),{method:'POST',body:JSON.stringify({records:[{fields}],typecast:true})});await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_REPORTES)+'/'+id,{method:'PATCH',body:JSON.stringify({fields:{Estado:'Confirmado'}})});toast('Pago confirmado.');";
-  const reportNew = "const processed=await adminFetch('/.netlify/functions/process-payment-report',{method:'POST',body:JSON.stringify({reportId:id,decision:'approve'})});toast(processed.message||'Pago confirmado, recibo procesado y acceso sincronizado.');";
-  if (html.includes(reportOld)) html = html.replace(reportOld, reportNew);
-
-  const rejectOld = "await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_REPORTES)+'/'+id,{method:'PATCH',body:JSON.stringify({fields:{Estado:'Rechazado'}})});toast('Pago rechazado.')";
-  const rejectNew = "const processed=await adminFetch('/.netlify/functions/process-payment-report',{method:'POST',body:JSON.stringify({reportId:id,decision:'reject'})});toast(processed.message||'Pago rechazado y acceso sincronizado.')";
-  if (html.includes(rejectOld)) html = html.replace(rejectOld, rejectNew);
+  // Confirmar/rechazar ya vienen protegidos desde admin.html; admin-payment-flow queda como defensa adicional.
 
   const headers = new Headers(response.headers);
   headers.delete('content-length');
diff --git a/netlify/edge-functions/admin-monthly-close.js b/netlify/edge-functions/admin-monthly-close.js
index 2977b02..88b8a30 100644
--- a/netlify/edge-functions/admin-monthly-close.js
+++ b/netlify/edge-functions/admin-monthly-close.js
@@ -12,9 +12,7 @@ export default async (request, context) => {
   const newClose = "async function runClose(){if(window.vlaCloseBusy)return;const btn=document.getElementById('close-btn');let dry=null;try{window.vlaCloseBusy=true;btn.disabled=true;btn.textContent='Preparando cierre...';dry=await adminFetch('/.netlify/functions/monthly-close',{method:'POST',body:JSON.stringify({dryRun:true})});if(dry.closeStatus==='already-closed')throw new Error(`El mes ${dry.month} ya fue cerrado.`);if(dry.closeStatus==='in-progress')throw new Error(`Ya existe un cierre de ${dry.month} en proceso.`);if(dry.repairAvailable&&dry.repairOperationId){if(!confirm(`Existe un cierre parcial de ${dry.month}. ¿Desea ejecutar la reparación automática antes de continuar?`))throw new Error('El cierre parcial debe repararse antes de continuar.');btn.textContent='Reparando cierre...';const repaired=await adminFetch('/.netlify/functions/monthly-close',{method:'POST',body:JSON.stringify({action:'repair',month:dry.month,operationId:dry.repairOperationId})});toast(repaired.message||'Cierre parcial reparado.');await loadAll(true);return}const v=dry.validation||{};const ok=await showCloseReview(v);if(!ok)return;btn.textContent='Verificando respaldo...';await adminFetch('/.netlify/functions/audit-snapshot',{method:'POST',body:JSON.stringify({month:dry.month})});const finalCheck=await adminFetch('/.netlify/functions/monthly-close',{method:'POST',body:JSON.stringify({dryRun:true,month:dry.month})});if(finalCheck.planHash!==dry.planHash)throw new Error('Los datos cambiaron durante la revisión. No se cerró el mes. Presione nuevamente Cierre de Mes para revisar los valores actualizados.');btn.textContent='Cerrando y verificando...';const done=await adminFetch('/.netlify/functions/monthly-close',{method:'POST',body:JSON.stringify({confirmed:true,month:dry.month,planHash:finalCheck.planHash})});alert(`Cierre completado y verificado.\nMes: ${done.month}\nPropietarios: ${done.updatedCount}\nPagos cerrados: ${done.paymentsClosedCount}${done.warning?`\nAdvertencia: ${done.warning}`:''}`);await loadAll(true)}catch(e){if(e.data&&e.data.repairAvailable&&e.data.repairOperationId){const repair=confirm(`${e.message}\n\n¿Ejecutar ahora la reparación automática protegida?`);if(repair){try{btn.textContent='Reparando cierre...';const repaired=await adminFetch('/.netlify/functions/monthly-close',{method:'POST',body:JSON.stringify({action:'repair',month:(e.data.month||(dry&&dry.month)),operationId:e.data.repairOperationId})});toast(repaired.message||'Reparación completada.');await loadAll(true);return}catch(repairError){toast(repairError.message,true);return}}}toast(e.message,true)}finally{window.vlaCloseBusy=false;btn.disabled=false;btn.textContent='📆 Cierre de Mes'}}";
   if (html.includes(oldClose)) html = html.replace(oldClose, newClose);
 
-  const oldExpenseCall = "await adminFetch('/.netlify/functions/airtable/'+encodeURIComponent(TABLE_GASTOS),{method:'POST',body:JSON.stringify({records:[{fields:{Concepto:concept,Monto:amount,'Tipo de Gasto':type,Frecuencia:freq,Propietarios:selected,'Forma de Pago':mode}}],typecast:true})});";
-  const newExpenseCall = "await adminFetch('/.netlify/functions/admin-expense',{method:'POST',body:JSON.stringify({action:'create',concept,amount,type,mode,frequency:freq,ownerIds:selected})});";
-  if (html.includes(oldExpenseCall)) html = html.replace(oldExpenseCall, newExpenseCall);
+  // La creación de gastos usa el endpoint dedicado directamente en admin.html.
 
   html = html.replace("/.netlify/functions/system-health'", "/.netlify/functions/system-health-advanced'");
   if (!html.includes('/verificar-respaldo.html')) {
